services:
  - type: web
    name: taixiu-bot
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: RENDER_URL
        fromService:
          name: taixiu-bot
          property: webUrl
